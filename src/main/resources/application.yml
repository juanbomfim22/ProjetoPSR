jwt:
  secret: ${JWT_SECRET:vazio}
  expiration: ${JWT_EXPIRATION:60000}
app:
  cors: 
    allowedOrigins: http://localhost:8080
  auth:
    tokenSecret: 04ca023b39512e46d0c2cf4b48d5aac61d34302994c87ed4eff225dcf3b0a218739f3897051a057f9b846a69ea2927a587044164b7bae5e1306219d50b588cb1
    tokenExpirationMsec: 60000 #864000000
#  oauth2:
#    # After successfully authenticating with the OAuth2 Provider,
#    # we'll be generating an auth token for the user and sending the token to the
#    # redirectUri mentioned by the client in the /oauth2/authorize request.
#    # We're not using cookies because they won't work well in mobile clients.
#    authorizedRedirectUris:
#      - http://localhost:3000/oauth2/redirect
#      - myandroidapp://oauth2/redirect
#      - myiosapp://oauth2/redirect
spring:
  security:
    oauth2:
      client:
        registration:
          # facebook url must be HTTPS
          facebook:
            client-id: '7661648650576281'
            client-secret: edd41a9f524137670f23fa056b2f64e4
            redirect-uri: "{baseUrl}/oauth2/callback/{registrationId}"
            scope:
              - email
              - public_profile
          google:
            client-secret: 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
            client-id: 947756629386-j2n26mn95sp9orfo93oimcj8q0lne94o.apps.googleusercontent.com
            redirect-uri: "{baseUrl}/oauth2/callback/{registrationId}"
            scope:
              - profile
              - email
          github:
            clientId: a93038f9c36c05b4f007
            clientSecret: 4aa882d693be60dd3e8f1236a8b62c96a07b8a02
            redirect-uri: "{baseUrl}/oauth2/callback/{registrationId}"
            scope:
              - user:email
              - read:user
#        provider:
#          facebook:
#            authorizationUri: https://www.facebook.com/v3.0/dialog/oauth
#            tokenUri: https://graph.facebook.com/v3.0/oauth/access_token
#            userInfoUri: https://graph.facebook.com/v3.0/me?fields=id,first_name,middle_name,last_name,name,email,verified,is_verified,picture.width(250).height(250)

  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
  data:
    mongodb:
      database: iclass
      uri: mongodb+srv://iclass-backend:OR1SMvj6dNSfmdea@estudos.g99vo.gcp.mongodb.net
  profiles:
    active: ${APP_PROFILE:dev}

